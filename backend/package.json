{
  "name": "keralance-hub-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun src/db/migrate.ts",
    "db:seed": "bun src/db/seed.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@t3-oss/env-core": "^0.13.11",
    "drizzle-orm": "^0.33.0",
    "hono": "^4.6.0",
    "jose": "^5.6.3",
    "pino": "^9.3.2",
    "postgres": "^3.4.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.24.2",
    "pino-pretty": "^11.2.2"
  }
}
